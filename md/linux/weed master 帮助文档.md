---
title: "weed master 帮助文档"
date: 2026-06-04
tags: ["linux"]
---

# SeaweedFS `weed master` 帮助文档（精简版）

本文档严格对照 `weed help master`​ 输出，**保留英文原文 + 对照中文翻译**，格式规范、便于查阅。

---

## 用法（Usage）

**原文**：  
Usage: weed master -port=9333

start a master server to provide volume=>location mapping service and sequence number of file ids

**翻译**：  
用法：weed master -port=9333

启动一个主节点服务器，提供 **卷 → 存储位置映射服务** 以及 **文件ID序列号分配**。

---

## 配置文件说明

**原文**：  
The configuration file "security.toml" is read from ".", "$HOME/.seaweedfs/", "/usr/local/etc/seaweedfs/", or "/etc/seaweedfs/", in that order.

The example security.toml configuration file can be generated by "weed scaffold -config=security"

**翻译**：  
配置文件 `security.toml` 会按以下顺序读取：  
当前目录 → 家目录下的 .seaweedfs → /usr/local/etc/seaweedfs → /etc/seaweedfs

可通过以下命令生成 `security.toml`​ 示例配置：  
​`weed scaffold -config=security`

---

## 单主节点模式说明

**原文**：  
For single-master setups, use -peers=none to skip Raft quorum wait and enable instant startup.  
This is ideal for development or standalone deployments.

**翻译**：  
对于**单主节点部署**，使用 `-peers=none` 可以跳过 Raft 一致性等待，实现快速启动。  
该模式非常适合开发环境或单机部署。

---

## 许可证文件加载顺序

**原文**：  
License file loading order:

1. -license flag path (if provided)
2. SEAWEED_LICENSE environment variable (if set)
3. Standard locations: "./seaweed-license.json", "$HOME/.seaweedfs/seaweed-license.json", "/usr/local/etc/seaweedfs/seaweed-license.json", "/etc/seaweedfs/seaweed-license.json"

**翻译**：  
许可证文件加载优先级：

1. ​`-license` 参数指定的路径（如果提供）
2. 环境变量 `SEAWEED_LICENSE`（如果设置）
3. 标准路径：

   - ./seaweed-license.json
   - $HOME/.seaweedfs/seaweed-license.json
   - /usr/local/etc/seaweedfs/seaweed-license.json
   - /etc/seaweedfs/seaweed-license.json

---

# 默认参数对照表（Default Parameters）

|参数名|英文原文|中文翻译|
| ---------------------------------| --------------------------------------------------------------------------------------------------------------------------------------------| -----------------------------------------------------------------------|
|-cpuprofile string|cpu profile output file|CPU 性能分析输出文件|
|-debug|serves runtime profiling data via pprof on the port specified by -debug.port|通过 pprof 在调试端口提供运行时性能分析数据|
|-debug.port int|http port for debugging (default 6060)|调试用 HTTP 端口（默认 6060）|
|-defaultReplication string|Default replication type if not specified.|未指定时使用的默认副本策略|
|-disableHttp|disable http requests, only gRPC operations are allowed.|禁用 HTTP 请求，仅允许 gRPC 操作|
|-electionTimeout duration|election timeout of master servers (default 10s)|主节点选举超时时间（默认 10 秒）|
|-garbageThreshold float|threshold to vacuum and reclaim spaces (default 0.3)|空间回收阈值（默认 0.3）|
|-heartbeatInterval duration|heartbeat interval of master servers, and will be randomly multiplied by [1, 1.25) (default 300ms)|主节点心跳间隔（默认 300ms）|
|-ip string|master <ip>|<server> address, also used as identifier (default "192.168.254.253")|
|-ip.bind string|ip address to bind to. If empty, default to same as -ip option.|绑定的 IP 地址，为空则与 -ip 一致|
|-license string|path to the license file|许可证文件路径|
|-maxParallelVacuumPerServer int|maximum number of volumes to vacuum in parallel per volume server (default 1)|每个卷服务器并行清理的最大卷数（默认 1）|
|-mdir string|data directory to store meta data (default "/tmp")|元数据存储目录（默认 /tmp）|
|-memprofile string|memory profile output file|内存性能分析输出文件|
|-metrics.address string|Prometheus gateway address <host>:<port>|Prometheus 网关地址|
|-metrics.intervalSeconds int|Prometheus push interval in seconds (default 15)|Prometheus 指标推送间隔（默认 15 秒）|
|-metricsIp string|metrics listen ip. If empty, default to same as -ip.bind option.|监控指标监听 IP|
|-metricsPort int|Prometheus metrics listen port|Prometheus 监控指标监听端口|
|-peers string|all master nodes in comma separated ip:port list, example: 127.0.0.1:9093,127.0.0.1:9094,127.0.0.1:9095; use 'none' for single-master mode|主节点集群列表，逗号分隔；填 `none` 为单主模式|
|-port int|http listen port (default 9333)|HTTP 监听端口（默认 9333）|
|-port.grpc int|grpc listen port|gRPC 监听端口|
|-raftBootstrap|Whether to bootstrap the Raft cluster|是否初始化 Raft 集群|
|-raftHashicorp|use hashicorp raft|使用 HashiCorp 版本的 Raft|
|-resumeState|resume previous state on start master server|启动时恢复上一次的运行状态|
|-telemetry|enable telemetry reporting (default true)|启用遥测上报（默认开启）|
|-telemetry.url string|telemetry server URL to send usage statistics (default "https://telemetry.seaweedfs.com/api/collect")|遥测数据上报地址|
|-volumePreallocate|Preallocate disk space for volumes.|为卷预分配磁盘空间|
|-volumeSizeLimitMB uint|Master stops directing writes to oversized volumes. (default 30000)|卷大小上限（MB），超过则停止写入（默认 30000）|
|-whiteList string|comma separated Ip addresses having write permission. No limit if empty.|拥有写入权限的 IP 白名单，逗号分隔|

---

### 总结

- 这是 **SeaweedFS Master 节点** 的完整帮助说明
- 支持**集群模式**和**单机快速模式（-peers=none）**
- 包含配置文件、许可证、参数的**中英对照**
- 可直接保存为 `weed-master.md` 文档使用
