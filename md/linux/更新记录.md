---
title: "更新记录"
date: 2026-06-04
tags: ["linux"]
---

#date/2024-09-05 20:53:10# #lastmod/2024-09-05 20:53:10#

## 更新记录

- ![a0e51336ee85a57fef6bf8ed663bc832fa75c4e3](https://md.kongseek.com/ea49a97dd465bec7be9c602a64fcd5d6bcf356365ead5ce08eea69266a5d5f1e.jpg)
- 带=- qq发送超时
- ACCESS_TOKEN_GIT/bd80530c25a45ea634a05113c836acaf.png
- 整理文件
- 添加腾讯服务器部署
- 重置所有md的修改时间为创建时间
- 测试keep主题
- 启用图片大图查看模式 +1
- 将构建完成提醒脚本从 js 修改为 go
- 测试 hexo 部署到华为 HK 服务器
- 修改错误图片
- 关闭文章封面图片
- 随机诗词 api 修改为 https
- 添加新玩具: 随机诗词
- 汉语新解 api 修改为 https
- 添加新玩具: 汉语新解
- 关闭图片懒加载
- 在背景图加载完成后直接关闭加载页但是无法滚动 +1
- 在背景图加载完成后直接关闭加载页但是无法滚动
- 在背景图加载完成后直接关闭加载页
- 修改顶部导航栏为新标签页打开
- 创建自定义 html 页面
- 修改 hexo 的 default_cover 和 background 为 https://api.[已脱敏].cc/random-image 随机 api 接口
- 测试下 gif 图片上传

  ![d92e8aea1e309cc6fe60cfa4680c6716d7b88a9a](https://md.kongseek.com/d92e8aea1e309cc6fe60cfa4680c6716d7b88a9a.gif)
- 修改 github 部署服务器时区为上海
- 同时部署到 serv00 和 RN，并在完成时发送 QQ 信息 +3
- 测试部署 hexo 到 serv00
- deploy.yml 流程修改为在 github 构建，将构建好的文件夹传到服务器，传之前先删除原 public
- 重试机制有问题，不要了
- deploy.yml 添加重试机制
- 懒加载时使用网络图片会 404，将图片添加到 butterfly 的 source/img 中
- 启用图片懒加载
- 回退为 css
- 将修改代码块透明度的代码从 css 转为 js +1
- 修改代码块透明度 +2
- 修改 deploy.yml 的部署命令，不再使用更新，直接先删除，再重新克隆项目进行部署 +6
- 回退 deploy.yml 一下 +1
- 修改 deploy.yml 的部署命令，不再使用更新，直接先删除，再重新克隆项目进行部署 +2
- typora 导出长时间未响应
- 添加随机背景图
- 修改文章的顶部区域高度为 200px + 1
- 修改文章的顶部区域高度为 200px
- 修复主页高度错误设置为 150px
- 设置所有卡片的背景透明度为 0.7，去除主页为的 top_img, 修改为透明背景
- 修复文章页白色背景
- 引用自定义 css
- 创建自定义的 css 文件
- 修改比较脚本
- 工作流两次构建之间等待 5 秒
- 添加 *config.butterfly.yml 和 主题目录中* config.yml 的比较脚本，将 _config.yml 中多出的键值添加到 _config.butterfly.yml
- footer 透明化
- 添加背景图
- 两个版本的 /themes/butterfly/layout/includes/header/index.pug 文件不同报错
- 两个版本的 _config.butterfly.yml 不同，复制备份一份 master-4.13.0 的配置文件
- 将主题 butterfly 版本从 master-4.13.0 变更为 dev-5.0.0-b1
- 修改提交信息，时间精确到秒
- 2
- 1
- 开始测试
- 修改 Deploy.yml 定义一个统一的组，确保工作流顺序执行，等待前一个工作流执行完成，而不是取消，保证同一时间只能有一个工作流实例运行，后续触发的工作流会等待当前工作流完成后再执行。
- git 忽略.idea
- 重新 Deploy.yml
- 测试是不是因为提交和推送临时 clone 的 main 分支后，仍然停留在 tmp 文件夹内导致 tmp 文件夹被占用 1
- 测试是不是因为提交和推送临时 clone 的 main 分支后，仍然停留在 tmp 文件夹内导致 tmp 文件夹被占用
- 删除 tmp 时显示文件夹被占用，多删几次，选了 3 次
- 不知道什么错误，再试一下
- 将 hexo 项目移到 article 的 hexo 分支，重新设置自动推送逻辑
- hexo 配置 mermaid 流程图编写格式同步 typora
- hexo butterfly 开启 mermaid 流程图渲染
- 测试 markdown 流程图解析
- 修改随机图 api 的图片质量
- 修改 typora 推送脚本，对于主仓库不再只提交对 source/_posts 的修改，而是提交整个仓库的修改
- 更改后的随机图 api 会在浏览器缓存只显示相同图片
- 更换随机图 api
- deploy hexo generate 构建两次防止一些乱七八糟的 bug
- 直接服务器构建 解决了白屏
- 又白屏，不知道原因
- deploy 去除重复命令
- deploy 检测远程比本地多的提交数是否符合预期，不符合继续循环，不直接退出
- deploy 检测远程比本地多的提交数是否符合预期
- deploy 获取远程比本地多的提交数
- deploy 循环检测更新 修改循环时间为 2 秒 +1
- deploy 循环检测更新 修改循环时间为 2 秒
- 白屏
- deploy 循环检测更新测试 +1
- deploy 循环检测更新测试
- 限制随机图片大小 +2
- 限制随机图片大小 +1
- 限制随机图片大小
- 页面空白了 +1
- 页面空白了
- 添加备案信息
- 去除公告
- 修改文章 url 的生成规则 +2
- 修改文章 url 的生成规则 +1
- 修改文章 url 的生成规则
- 加载动画有点问题
- 安装 fs-extra 模块，添加复制自定义主题文件的 js 脚本
- 去除标签页和分类页的顶部图
- 将文章界面上面的图片去掉
- 构建前删除上传的 public
- 为 Butterfly 主题添加随机图片 api 支持
- 添加自定义主题文件夹
- 修改主题 butterfly 安装方式
- 跳过指定文件夹成功啦
- 跳过方法错了，直接在需要通过的文件夹和文件名字前添加 _
- 跳过路径还是错了，再次修改
- 跳过路径还是错了，再次修改
- 改成 aljp 的配置文件了，被远程覆盖了
- 成功了，但是跳过文件夹 配置错误，修改
- 第二次也失败，修改 Deploy 再次测试
- 测试 跳过文件夹 第二次，失败一次
- 测试 跳过文件夹
- 不转发，直接连接 aljp
- 完美构建自动化发布
- 测试 send.mjs 构建时间
- 测试 github action
