---
title: "AAA(这个就用来测试一下思源语法和使用方法)"
date: 2026-06-04
tags: ["misc"]
---

1. **移除 zip 相关逻辑**：

   - 删除 `checkAndInstallZip `函数（不再依赖 zip 命令）
   - 移除所有 `exec.Command("zip", ...) `调用
2. **适配 7z 特性**：

   - 压缩包后缀从 `.zip`​ 改为 `.7z`（符合 7z 格式规范）
   - 排除参数从 zip 的 `-x`​ 改为 7z 的 `-x!绝对路径` 格式
   - 复用 7zip.go 的重试机制和多线程压缩能力
3. **保留原有校验逻辑**：

   - 继续使用 `ls -lh `查看压缩包大小
   - 保留 `sha256sum `校验压缩包完整性
4. **路径适配**：

   - 排除路径拼接为绝对路径，确保 7z 能准确识别排除规则
